// slots/slots.js
import * as THREE from 'three';

export function criarSlots(scene) {
    const slots = [];
    // criar grupos, cilindros
    return slots;
}

export function atualizarSlots(slots) {
    // animações
}

export function interagirSlot(slots, jogador, collectibles) {
    // verificar proximidade e combinação
}