// collectibles/collectibles.js
import * as THREE from 'three';

export function criarColecionaveis(scene) {
    const items = [];
    return items;
}

export function verificarRecolha(camera, collectibles, jogador) {
    // lógica de clique
}